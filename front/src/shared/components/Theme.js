import { createTheme } from '@mui/material';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#61dafb',
        },
        secondary: {
            main: '#da61bf',
        },
        palette: {
            bookCard: {
                even: '#ffcdd2',
                odd: '#e1bee7',
            },
        },
    },
});
